This folder contains my code for part 2 of the third assignment, the Naive Bayes Classifier.
It also containss my report, and all the neccessary data files for the code.
To run my code, after extracting the zip file, simply follow these steps:

- Open a new console and copy and paste (ctrl-SHIFT-v) the following commands into it
- cd 'Downloads/COMP307 Assignment 3/'
- python a3Part2.py spamLabelled.dat spamUnlabelled.dat

The code should run as expected and generate the output which is stored in sampleoutput.txt

Libraries used:
- sys
- pandas
- numpy
To install these libraries if they are absent (shouldn't be needed)
- Use the pip install command
e.g. pip install sys

